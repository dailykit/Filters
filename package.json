{
  "name": "@dailykit/filters",
  "version": "1.0.0",
  "description": "For filtering of data",
  "main": "index.js",
  "scripts": {
    "test": "export NODE_ENV=test || set \"NODE_ENV=test\" && mocha ./tests/*/*.test.js --timeout 50000",
    "test-watch": "nodemon --exec \"npm run test\""
  },
  "author": "Suyash Awasthi",
  "license": "ISC",
  "dependencies": {
    "lodash": "^4.17.15"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^7.0.1",
    "nodemon": "^2.0.2"
  }
}
